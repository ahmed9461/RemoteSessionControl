# Replace control.example.com with the real DNS name that points to this VPS.
# Caddy automatically obtains/renews TLS certificates and proxies WebSocket upgrades.

control.example.com {
    encode zstd gzip
    reverse_proxy 127.0.0.1:8000
}
