[project]
name = "remote-session-control"
version = "0.1.0"
description = "Consent-based temporary remote session control with channel adapters"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
target-version = "py312"
line-length = 100
