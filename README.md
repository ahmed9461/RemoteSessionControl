# RemoteSessionControl

Consent-based temporary remote session platform. Telegram is the first channel adapter; Windows/macOS device clients connect through a versioned server protocol.

> Security posture: no stealth, no persistence, no credential collection. Device access requires explicit pairing and an active time-limited session.
